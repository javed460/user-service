# user-service
A Spring Boot-based microservice that manages user identity data. This service is designed to act as an external identity provider for Keycloak via a Custom User Storage Provider.


The service will start on http://localhost:8082.

## 🛠 Tech Stack
Framework: Spring Boot 3.x

Language: Java 21

Database: H2 (In-Memory)

Build Tool: Maven

## 🔑 Keycloak Integration
This service provides the data consumed by the keycloak-custom-provider.